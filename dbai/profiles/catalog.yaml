# DBAI phase-profile catalog (PROFILE-01, doc-18 §5).
#
# A profile pins EXACTLY which module, bootstrap, platform and tools prepared an
# environment, so teaching/exam behavior cannot drift through implicit upgrades.
# Profiles here are `candidate` — they are frozen and qualified by later tasks
# (PROFILE-05/06/08/10/11, capacity + boot-time). A changed tool/module/
# bootstrap input requires a NEW profile version (never edit a published one).
#
# Placeholder tokens (TBD/latest/…) are rejected by validate-profiles.py. A
# value that is genuinely not decided yet is `null` (explicitly pending), which
# is allowed for candidates but never for a qualified reference.
schema: dbai/profile-catalog/v1
baseline_version: "1.0.0"
ec2_console_version: 7a0fa4f2b5512e8b34d8dee11e9779c806ad2752   # candidate pin
bootstrap_sha256: sha256:8c9b26aa327ceb485a27fb00bb13bb585d6f32eb18356ea93590cf64ed48f9bc

profiles:
  - name: foundations
    version: foundations-0.1.0
    status: candidate
    phases: [S2, S3]
    ubuntu: "24.04"
    ami: ami-03957e4cfe042cca1        # Ubuntu 24.04 LTS, eu-west-1
    instance_size: t3.small
    module_revision: null              # pending RECOVERY-01
    bootstrap_template: dbai/profiles/foundations/cloud-init.yaml.tftpl
    bootstrap_sha256: sha256:5a209fb7d7a4305067f4636ee76d0c5a8f128cebe9c8e82b6fc56531e13937bd
    tools:                             # version pins are future deliverables (null=pending)
      ssh: distro
      git: distro
      uv: null
      remote_editor: supported
    verification_evidence: PROFILE-05  # qualifies capacity + boot time
    student_excluded: "App repairs, feature/test solution, student-authored CI"

  - name: containers
    version: containers-0.1.0
    status: candidate
    phases: [S4, S5, S6, S7]
    ubuntu: "24.04"
    ami: ami-03957e4cfe042cca1
    instance_size: t3.medium
    module_revision: null
    bootstrap_template: dbai/profiles/containers/cloud-init.yaml.tftpl
    bootstrap_sha256: sha256:137bb7d571dd0434ecb589a0d080a6e45b9f5a69efdb7425bb87a173d817829a
    tools:                             # adds Docker/Compose over foundations (version pins pending)
      ssh: distro
      git: distro
      uv: null
      docker: null
      docker_compose: null
    verification_evidence: PROFILE-08
    student_excluded: "Student Dockerfile/Compose work, release workflow, S6 edits and recovery evidence"

  # operations-0.3.0 (PROFILE-11 correction): same bootstrap/tools as 0.2.0, root volume
  # 25 -> 30 GB (0.2.0 fails the S11-S12 worst-case PVC limit, 93.1 %) and TCP 80 from
  # the VM's own EIP so in-cluster probes of wb.<EIP>.sslip.io work (S10 hairpin).
  - name: operations
    version: operations-0.3.0
    status: candidate
    phases: [S8, S9, S10, S11, S12, S13]
    k3s_enabled: true
    ubuntu: "24.04"
    ami: ami-03957e4cfe042cca1
    instance_size: t3.large
    root_volume_gb: 30
    web_ingress_self: true
    module_revision: null
    bootstrap_template: dbai/profiles/operations/cloud-init.yaml.tftpl
    bootstrap_sha256: sha256:45df3946210e27518ce159059274e7573c266ce710252d3df8031e793d4d70ad
    tools:
      ssh: distro
      git: distro
      uv: null
      docker: null
      k3s: v1.30.5+k3s1
      kubectl: v1.30.5
      helm: v3.15.4
    verification_evidence: PROFILE-11            # S11-S12 peak re-measured on 30 GB (QUALIFICATION-S11-S12.md)
    student_excluded: "Student manifests/values/auth, monitoring dashboard, MLflow runs, retrieval releases"

  - name: operations
    version: operations-0.2.0                 # PROFILE-04: S8 k3s transition = new pinned revision
    status: superseded                        # by operations-0.3.0; still resolvable by exact version
    phases: [S8, S9, S10, S11, S12, S13]
    k3s_enabled: true                         # S8 boundary introduces k3s (PROFILE-04)
    ubuntu: "24.04"
    ami: ami-03957e4cfe042cca1
    instance_size: t3.large
    module_revision: null
    bootstrap_template: dbai/profiles/operations/cloud-init.yaml.tftpl
    bootstrap_sha256: sha256:45df3946210e27518ce159059274e7573c266ce710252d3df8031e793d4d70ad
    tools:                                    # pinned k3s/kubectl/Helm for S8/S9 (uv/docker carried, pins pending)
      ssh: distro
      git: distro
      uv: null
      docker: null
      k3s: v1.30.5+k3s1
      kubectl: v1.30.5                         # k3s-bundled, symlinked
      helm: v3.15.4
    verification_evidence: PROFILE-09          # capacity/boot qualification (S8-S10) stays with PROFILE-09
    student_excluded: "Student manifests/values/auth, monitoring dashboard, MLflow runs, retrieval releases"

  # Practical-exam selections reuse the SAME qualified tool versions/repos; they
  # add no new tools and must never resolve through a mutable reference.
  - name: exam-s7-practical
    version: exam-s7-practical-0.1.0
    status: candidate
    kind: practical
    phases: [S7]
    selects: [foundations-0.1.0, containers-0.1.0]
    instance_size: t3.medium
    module_revision: null
    verification_evidence: PROFILE-06
    student_excluded: "No pre-solved faults, no replacement grading repo"

  - name: exam-s14-practical
    version: exam-s14-practical-0.1.0
    status: candidate
    kind: practical
    phases: [S14]
    selects: [operations-0.3.0]
    instance_size: t3.large
    module_revision: null
    verification_evidence: PROFILE-10
    student_excluded: "No hidden automatic repair or fresh substitute project"
